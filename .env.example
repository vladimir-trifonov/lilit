# Database (required)
# For local development (host):
DATABASE_URL="postgresql://lilit:lilit@localhost:5434/lilit"
# For Docker Compose (container):
# DATABASE_URL="postgresql://lilit:lilit@postgres:5432/lilit"

# Google Gemini API key (optional — enables Gemini as an additional provider)
# Get from: https://aistudio.google.com/apikey
GOOGLE_GENERATIVE_AI_API_KEY=""

# Budget limit per pipeline run in USD (default: 10.0)
# DEFAULT_BUDGET_LIMIT="10.0"

# Ollama embedding server (optional — enables RAG memory system)
# For local development (host): http://localhost:11434
# For Docker Compose (container): http://ollama:11434
OLLAMA_BASE_URL="http://localhost:11434"
